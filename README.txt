You will need CRON, which is available both on Linux and Mac, for the program to run as soon as the computer starts.

The host_path varaiable will need to replace host_temp varaiable when switching over to running program from startup to where ever you decide to house the host file. 

